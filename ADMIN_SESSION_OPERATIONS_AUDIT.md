# 관리자 세션 관련 작업 시나리오 점검 보고서

## 개요
관리자가 세션 관련해서 수행하는 모든 작업들에 대한 비판적 점검 및 치명/중간 위험 수준 시나리오 분석

---

## 1. 세션 취소 (cancelSession)

### 작업 내용
- 특정 날짜의 특정 세션을 취소하고 모든 예약자에게 알림 전송

### 구현 상태
- ✅ 트랜잭션 적용: 예약 삭제, enrollment 복구, sessionReservations 업데이트 모두 트랜잭션 내
- ✅ 알림 발송: 트랜잭션 외부에서 Promise.allSettled로 처리
- ✅ sessionReservations에 `isCancelled: true` 설정

### 시나리오

#### ✅ 정상 케이스
1. 관리자가 세션 취소 버튼 클릭
2. 확인 다이얼로그 표시 (예약자 수, 알림 발송 안내)
3. 확인 시 `cancelSession` Cloud Function 호출
4. 트랜잭션 내에서:
   - 모든 예약 삭제
   - 각 enrollment의 `remainingReservations++` (그룹화하여 효율적으로)
   - `sessionReservations`의 `reservedCount--` 및 `isCancelled: true` 설정
5. 트랜잭션 성공 후 모든 예약자에게 알림 발송
6. 캘린더에서 취소 표시 (회색, 취소선, 빨간 원)

#### ⚠️ 중간 위험: 알림 발송 실패
- **문제**: 알림 발송이 실패해도 트랜잭션은 이미 커밋됨
- **영향**: 예약자는 세션이 취소되었지만 알림을 받지 못할 수 있음
- **현재 대응**: Promise.allSettled로 일부 실패해도 계속 진행, 로그 기록
- **권장**: 알림 발송 실패 시 재시도 큐에 추가하거나 관리자에게 알림

#### ⚠️ 중간 위험: 동시 취소 시도
- **문제**: 같은 세션을 두 관리자가 동시에 취소하려고 할 때
- **영향**: 트랜잭션 충돌 가능, 일부 예약만 취소될 수 있음
- **현재 대응**: Firestore 트랜잭션의 자동 재시도
- **권장**: UI에서 취소 중 상태 표시 및 중복 클릭 방지

---

## 2. 유저 추가 (관리자가 다른 사용자를 위해 예약 생성)

### 작업 내용
- 관리자가 세션 상세 화면에서 멤버를 선택하여 예약 추가
- `createReservation`에 `userId` 파라미터 전달

### 구현 상태
- ✅ 관리자 권한 검증: `assertAdminUid`로 확인
- ✅ 정책 우회: 관리자는 정원 초과/오픈 전/마감 전 우회 가능 (과거 날짜만 차단)
- ✅ 트랜잭션 적용: 예약 생성, enrollment 차감, sessionReservations 업데이트
- ✅ 알림 발송: 예약자에게 알림 전송

### 시나리오

#### ✅ 정상 케이스
1. 관리자가 "예약자 추가" 버튼 클릭
2. 멤버 선택 바텀시트 표시 (해당 코스에 등록한 멤버만, 이미 예약한 멤버 제외)
3. 멤버 선택 후 "추가하기" 클릭
4. `createReservation` 호출 (userId 포함)
5. 트랜잭션 내에서:
   - enrollment 검증 (해당 코스 등록 여부)
   - 정책 검증 (관리자는 과거 날짜만 차단)
   - 중복 예약 체크
   - 예약 생성
   - enrollment의 `remainingReservations--`
   - `sessionReservations`의 `reservedCount++`
6. 트랜잭션 성공 후 예약자에게 알림 발송

#### 🔴 치명 위험: 크레딧 없는 사용자 예약 생성
- **문제**: 관리자가 `remainingReservations = 0`인 사용자를 위해 예약 생성 시
- **현재 동작**: 
  - 정책 우회로 `enrollmentCanReserve = false`여도 예약 생성 가능
  - 하지만 트랜잭션 내에서 `remainingReservations--` 실행
  - `remainingReservations`가 음수가 될 수 있음
- **코드 위치**: `functions/src/index.ts:1587` - `tx.update(enrollmentDoc.ref, { remainingReservations: remainingReservations - 1 })`
- **영향**: 데이터 일관성 파괴, 사용자가 예약은 있지만 크레딧이 음수
- **권장**: 관리자 예약 생성 시에도 `remainingReservations`가 0 이상인지 확인, 또는 별도 플래그로 관리

#### ⚠️ 중간 위험: 정원 초과 예약 생성
- **문제**: 관리자가 정원을 초과하여 예약 생성 가능
- **현재 동작**: 정책 우회로 정원 초과해도 예약 생성
- **영향**: `sessionReservations.reservedCount > capacity` 상태 발생
- **권장**: 관리자도 정원 초과 시 경고 표시, 강제 생성 옵션 제공

#### ⚠️ 중간 위험: 동시 예약 생성
- **문제**: 같은 세션에 여러 관리자가 동시에 예약 추가
- **영향**: 트랜잭션 충돌, 일부만 성공할 수 있음
- **현재 대응**: Firestore 트랜잭션의 자동 재시도
- **권장**: UI에서 예약 추가 중 상태 표시

---

## 3. 유저 삭제 (관리자가 예약 취소)

### 작업 내용
- 관리자가 세션 상세 화면에서 특정 예약자의 예약을 취소

### 구현 상태
- ❌ **치명 문제**: 관리자가 취소할 때도 일반 사용자와 동일하게 1시간 전 제한 적용
- ❌ **치명 문제**: `cancelReservation`에서 `reservation.userId !== userId` 체크로 관리자 취소 불가

### 시나리오

#### 🔴 치명 위험: 관리자 예약 취소 불가
- **문제**: `cancelReservation` 함수에서 `reservation.userId !== userId`일 때 `permission-denied` 에러
- **현재 코드**:
  ```typescript
  if (reservation.userId !== userId) {
      throw new functions.https.HttpsError('permission-denied', '본인의 예약만 취소할 수 있습니다.');
  }
  ```
- **영향**: 관리자가 다른 사용자의 예약을 취소할 수 없음
- **권장**: 관리자 권한 확인 후 우회

#### 🔴 치명 위험: 1시간 전 제한
- **문제**: 관리자가 취소할 때도 세션 시작 1시간 전까지만 취소 가능
- **현재 코드**:
  ```typescript
  const closeAt = new Date(sessionStart.getTime() - 60 * 60_000);
  if (seoulNow.getTime() >= closeAt.getTime()) {
      throw new functions.https.HttpsError('failed-precondition', '세션 시작 1시간 전까지만 취소 가능합니다.');
  }
  ```
- **영향**: 긴급 상황에서 관리자가 예약을 취소할 수 없음
- **권장**: 관리자 권한 확인 후 시간 제한 우회

#### ⚠️ 중간 위험: 크레딧 복구 실패
- **문제**: enrollment를 찾지 못하거나 업데이트 실패 시 크레딧이 복구되지 않음
- **현재 대응**: 트랜잭션 내에서 처리하므로 일관성은 보장
- **권장**: enrollment가 없을 때 경고 로그

---

## 4. 멤버 등록 (enrollMemberToCourses)

### 작업 내용
- 관리자가 새 멤버를 등록하거나 기존 멤버에 코스 추가

### 구현 상태
- ✅ 트랜잭션 적용: 멤버 등록 시 enrollments 생성
- ⚠️ 확인 필요: 멤버 등록 시 기존 예약과의 충돌

### 시나리오

#### ✅ 정상 케이스
1. 관리자가 멤버 등록 화면에서 정보 입력
2. 코스 선택 및 설정 (횟수, 유효기간)
3. `enrollMemberToCourses` 호출
4. 트랜잭션 내에서:
   - User 생성/업데이트
   - 각 코스별 enrollment 생성
   - `remainingReservations = totalReservations`로 초기화

#### ⚠️ 중간 위험: 중복 등록
- **문제**: 같은 코스에 이미 등록된 멤버를 다시 등록할 때
- **현재 동작**: `existingEnrollmentIds.contains(courseId)`로 스킵
- **영향**: 중복 등록은 방지되지만, 기존 등록을 연장하는 의도였을 수 있음
- **권장**: 중복 등록 시 "이미 등록됨" 메시지 및 연장 옵션 제공

#### ⚠️ 중간 위험: 트랜잭션 실패 시 부분 등록
- **문제**: 여러 코스를 등록할 때 일부만 성공하고 일부는 실패
- **현재 동작**: 각 코스별로 try-catch로 처리, 실패해도 계속 진행
- **영향**: 일부 코스만 등록되고 일부는 실패할 수 있음
- **권장**: 전체 실패 시 롤백 또는 부분 성공 시 상세한 피드백

---

## 5. 등록 취소 (enrollment 만료 처리)

### 작업 내용
- 관리자가 멤버를 코스에서 제거 (유효기간을 과거로 설정)

### 구현 상태
- ✅ 히스토리 기록: `reenrollmentHistory`에 취소 기록 추가
- ⚠️ 확인 필요: 트랜잭션 처리 여부

### 시나리오

#### ✅ 정상 케이스
1. 관리자가 멤버 상세 화면에서 "코스에서 삭제" 클릭
2. 확인 다이얼로그 표시
3. `updateEnrollment` 호출
4. `validUntil`을 과거로 설정하여 만료 처리
5. 히스토리에 취소 기록 추가

#### 🔴 치명 위험: 활성 예약이 있는 상태에서 등록 취소
- **문제**: 멤버가 아직 사용하지 않은 예약이 있는데 등록을 취소할 때
- **현재 동작**: 등록 취소 시 예약 상태 확인 없음
- **영향**: 
  - 예약은 남아있지만 등록이 만료되어 예약 사용 불가
  - 데이터 일관성 파괴
- **권장**: 등록 취소 전 활성 예약 확인, 예약이 있으면 경고 또는 예약 취소 옵션 제공

#### ⚠️ 중간 위험: 트랜잭션 없이 업데이트
- **문제**: `_updateEnrollmentDirect`가 단순 `update`로 처리
- **영향**: 동시 업데이트 시 데이터 손실 가능
- **권장**: 트랜잭션 사용 또는 버전 체크

---

## 6. 재연장 (extension approve, reenrollment)

### 작업 내용
- 관리자가 연장 요청을 승인하거나 재등록 처리

### 구현 상태
- ✅ 연장 승인: `approveExtension`으로 `validUntil` 연장
- ⚠️ 확인 필요: 트랜잭션 처리 여부

### 시나리오

#### ✅ 정상 케이스 (연장 승인)
1. 관리자가 연장 요청 목록에서 "연장 승인하기" 클릭
2. `approveExtension` 호출
3. `validUntil`을 30일 연장
4. 연장 요청 상태를 `approved`로 변경

#### ✅ 정상 케이스 (재등록)
1. 관리자가 "재등록" 버튼 클릭
2. 재등록 다이얼로그에서 설정 입력
3. `reenrollment` 처리
4. 히스토리에 재등록 기록 추가

#### ✅ 정정: 연장(기간 연장)에서 횟수 조정은 하면 안 됨
- **사용자 요구사항/정책**: 연장은 “기간(validUntil)만” 연장하며, `remainingReservations`는 조정하지 않는다.
- **전제**: 애초에 “횟수가 남아있는 경우에만” 연장이 가능하도록 정책을 잡는다.
- **따라서**: 이전 버전 보고서의 “연장 승인 시 크레딧 미복구” 항목은 **문제 지적이 아니라 의도된 정책**이므로 삭제/정정한다.

#### ⚠️ 중간 위험: 트랜잭션 없이 업데이트
- **문제**: `updateEnrollment`가 단순 업데이트
- **영향**: 동시 업데이트 시 데이터 손실 가능
- **권장**: 트랜잭션 사용

---

## 중앙 정책 엔진(일괄 검증) 필요성: 결론은 “예, 다만 형태를 잡아야 함”

### 현 상태에서 이미 “엔진”은 존재함
- **클라이언트**: `lib/policies/reservation_policy_engine.dart` (UX/사전 잠금 표시 목적)
- **서버(최종 검증)**: `functions/src/index.ts` 내 `evaluateReservation(...)`

### 지금 남은 치명 이슈들이 발생한 근본 원인
- **권한/운영 예외가 ‘함수별로’ 다르게 구현됨**
  - 예: 예약 생성은 관리자 우회가 있고(`createReservation`), 예약 취소는 관리자 우회가 없음(`cancelReservation`)
- **정합성 불변식(invariant)이 ‘한 곳’에서 강제되지 않음**
  - 예: 관리자 예약 생성 우회로 `remainingReservations`가 음수가 될 위험

### “중앙화”를 할 때의 권장 방향(실행 가능한 형태)
- **서버에만 ‘단일 Source of Truth’로 강제**: Callable Functions 안에서 공통 모듈로 검증/예외정책을 통일
- **검증 결과를 구조화**: `{ allow: boolean, reason: code, overridesAllowed?: code[] }` 형태로 표준화
- **관리자 우회도 ‘명시적’으로**: “무엇을 우회했는지”를 로그/DB에 남겨 추적 가능하게

### 이번 보고서 기준, 중앙화 우선순위(효과 큰 순)
1. `cancelReservation`에 관리자 취소 정책(권한 + 시간제한 우회)을 `createReservation`과 동일한 틀로 정렬
2. 관리자 예약 생성 시 `remainingReservations` 음수 방지 불변식 강제
3. 등록 취소 시 “활성 예약 존재” 불변식 강제(경고/차단/연쇄취소 정책 결정)

---

## 종합 위험도 평가

### 🔴 치명 위험 (즉시 수정 필요)
1. **관리자 예약 취소 불가**: `cancelReservation`에서 관리자 권한 확인 없이 본인만 취소 가능
2. **관리자 예약 취소 시간 제한**: 1시간 전 제한이 관리자에도 적용됨
3. **크레딧 없는 사용자 예약 생성**: `remainingReservations`가 0 이하가 될 수 있음
4. **활성 예약이 있는 상태에서 등록 취소**: 데이터 일관성 파괴

### ⚠️ 중간 위험 (우선순위 높음)
1. **정원 초과 예약 생성**: 관리자가 정원을 초과하여 예약 생성 가능
2. **동시 작업 충돌**: 여러 관리자가 동시에 작업할 때 트랜잭션 충돌
3. **알림 발송 실패**: 알림 실패 시 재시도 메커니즘 없음
4. **등록 취소/연장 시 트랜잭션 부재**: 데이터 일관성 보장 어려움

---

## 권장 수정 사항

### 1. 관리자 예약 취소 수정 (치명)
- `cancelReservation` 함수에 관리자 권한 확인 추가
- 관리자는 시간 제한 없이 취소 가능
- 관리자는 다른 사용자의 예약도 취소 가능

### 2. 크레딧 검증 강화 (치명)
- 관리자 예약 생성 시에도 `remainingReservations >= 0` 확인
- 또는 관리자 예약은 별도 플래그로 관리

### 3. 등록 취소 전 예약 확인 (치명)
- 등록 취소 전 활성 예약 확인
- 예약이 있으면 경고 또는 예약 취소 옵션 제공

### 4. 트랜잭션 적용 확대 (중간)
- 등록 취소, 연장 승인, 재등록 시 트랜잭션 사용

### 5. 알림 재시도 메커니즘 (중간)
- 알림 발송 실패 시 재시도 큐에 추가
- 또는 관리자에게 알림 발송 실패 알림

